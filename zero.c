#include<stdio.h>
int main (void)
{
    int num;
    printf("Enter the number: \n");
    scanf("%d", &num);
    if (num == 0)
    printf("Given number is zero");
    else
    printf("Given number is not zero");
}
